{
  "name": "juke-cafe-site",
  "version": "1.0.0",
  "description": "JuKe Café · website met Decap CMS",
  "private": true,
  "scripts": {
    "start": "npx @11ty/eleventy --serve --port=8080",
    "build": "npx @11ty/eleventy",
    "debug": "DEBUG=Eleventy* npx @11ty/eleventy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
